# 2026-08-12

## 公众号发布方案（YOLOv1 笔记）
- 用户运营学术论文解读类个人公众号，目标读者为泛学术人群；README.md 为首篇待发文案。
- 评估结论：全文折合 6000+ 字（3300 中文字 + 公式/表格/代码），自标 20 分钟，建议拆为上下两篇（上 2500–3200 字，下 2800–3500 字），间隔 3–4 天、晚 8–10 点发布。
- 产出《发布方案.md》：含拆分方案、悬念衔接话术、排版清单（两级标题/段落≤4行/公式转图/表格瘦身/开头三件套/结尾四件套）及发布前检查清单。
- 参考样本：量子位、卡兹克、Datawhale、财联社AI daily 四篇的共同手法——短段落、加粗金句独立成段、引用块、结尾三连引导。

## 上篇秀米成稿
- 按发布方案产出《上篇_秀米成稿.md》：含 3 个备选标题、封面建议、开头三件套、五个命题式板块、4 处金句、2 处配图标注、结尾下篇预告卡（无关注引导）；段落均压至移动端 ≤4 行。下篇成稿待用户确认后再出。

## 本地排版 HTML 生成
- 因个人订阅号无 AppSecret，无法走公众号 API；改为生成可直接复制粘贴到公众号编辑器的本地 HTML。
- 新增 `build_wechat_html.py` + `render_math.cjs`：用 pandoc 将 `wechat-article.md` 转为 HTML 片段，调用本机 Chrome 通过 CDP 把 14 处 LaTeX 公式逐条渲染为高清 PNG，再把 6 张本地配图转成 base64，最终内联微信公众号安全样式，输出单文件 `wechat-article.html`（约 6.2 MB）。
- 关键验证：0 个 `<script>`、0 个 `<style>`、0 个外部 http 引用、14 个公式图、6 个 base64 配图，可直接在浏览器打开预览或整篇复制到公众号后台。

## 封面设计（上篇）
- 用 Pillow（隔离 venv：~/.workbuddy/binaries/python/envs/default）渲染 3 套 900×383 头条封面：cover_A_dark（深藏青+红色检测框，主推）、cover_B_light（米白学术风）、cover_C_blue（克莱因蓝+明黄撞色）。
- 统一视觉语言：7×7 网格 + YOLO 风格检测框（角标刻度 + "object 0.99" 标签 chip）；主标题"只看一眼"（微软雅黑 Bold 104px）；渲染脚本 render_cover.py 可复用改字改色。
